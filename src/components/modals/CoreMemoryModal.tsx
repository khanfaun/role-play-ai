import React from 'react';

// Chức năng của thành phần này đã được gộp vào ManagementModal.tsx (trước đây là JournalModal.tsx) như một tab mới.
// File này không còn được sử dụng và được giữ lại để tránh làm hỏng các giả định của hệ thống file.
const CoreMemoryModal: React.FC = () => null;

export default CoreMemoryModal;